import { FormularioOrden } from "@/components/formulario-orden";

export const metadata = { title: "Nueva orden" };

/** Alta de orden (TCI-24 en la interfaz). */
export default function NuevaOrdenPage() {
  return <FormularioOrden />;
}
